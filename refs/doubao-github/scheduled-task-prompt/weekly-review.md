请创建每周一次的个人记录回看任务；如果我还没有提供星期和执行时间，先询问后再创建。

每次执行时，读取 GitHub 仓库 `YOUR_GITHUB_USERNAME/YOUR_REPOSITORY` 下的 `.agents/skills/weekly-review/SKILL.md`，执行第一阶段。完整读取最近七天的 `daily/journal/` 与 `daily/inputs/`，识别有证据的 pattern、连接和变化，提出 1–3 个问题，并始终把 Review 提交到 `reviews/{YYYY}/{YYYYMM}/{开始日期}-{结束日期}-{关键词}.md`。关键词应简短、具体且来自本周材料；同一日期范围已有 Review 时更新原文件并保留原文件名。

任务结果返回回看摘要、问题、可能发展的方向、Review 路径与 commit 信息。不要在定时运行中直接生成文章或创建 Practice；等我回答或确认方向后，再按 Skill 继续。
