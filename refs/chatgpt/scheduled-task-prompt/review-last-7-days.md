这是个人记录系统的定期七天回看任务。读取 GitHub 仓库 `YOUR_GITHUB_USERNAME/YOUR_REPOSITORY` 下的 `.agents/skills/review-daily-entries/SKILL.md`，执行其中的回看流程。

按仓库根目录 `PROFILE.md` 中的时区计算最近七个日历日并包含今天；未填写时使用当前执行环境的本地日期，只有日期边界仍有歧义时才询问。读取对应日期范围内的 `daily/inputs/`，并仅在需要确认生活处境、行动变化或时间线时读取必要的 `daily/journal/`。范围跨月或跨年时检查相邻目录。

以具体日期和仓库相对路径作为证据，识别少量真正反复出现的问题、连接、变化和未完成之处，并给出最值得继续的 1–3 个方向。不要为了显得全面扫描整个仓库，不把短期情绪、孤立记录或 AI 推测写成稳定结论。

本次任务只在结果中返回回看内容，不创建文件，不自动建立 Practice，也不直接生成周记。如果某条线索可能发展为 Practice，先展示证据并留待我确认；如果材料不足，诚实说明缺少什么，不制造主题。
