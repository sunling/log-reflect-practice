这是突破信息茧房的定期探索任务。读取 GitHub 仓库 `YOUR_GITHUB_USERNAME/YOUR_REPOSITORY` 下的 `.agents/skills/bubble-breaker/SKILL.md`，执行其中的 Discovery / `discover` 流程。生成 1 个待我认真阅读、收听或观看的资源任务。找到并核实资源后，只在任务结果中返回该资源任务，不要提前入库；等我明确说“完成了”后，再按 Skill 中的 `complete` 流程完成 GitHub 入库。如果我另外提供了通知邮箱，并且当前环境已经连接且授权 Gmail，可以把同样的任务内容发送到该邮箱；否则不要发送邮件，也不要因此中断任务。
