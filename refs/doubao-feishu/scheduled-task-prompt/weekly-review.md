请创建每周一次的个人记录回看任务；如果我还没有提供星期和执行时间，先询问后再创建。

每次执行时，使用 Weekly Review Skill 完成第一阶段。完整读取飞书云盘中最近七天的 `daily/journal/` 与 `daily/inputs/`，识别有证据的 pattern、连接和变化，提出 1–3 个问题，并始终把 Review 上传到 `reviews > {YYYY} > {YYYYMM}`，文件名为 `{YYYYMMDD}-weekly-review.md`。

任务结果返回回看摘要、问题、可能发展的方向和飞书文件链接。不要在定时运行中直接生成文章或创建 Practice；等我回答或确认方向后，再按 Skill 继续。
